# Validate an existing workflow

Paste an n8n workflow in YAML or JSON and the agent runs validate_n8n_workflow to check it against the schema, confirm required attributes, and report errors and warnings before you import it.

TODO: Add the example implementation.
