# Code generated by ADL CLI v0.49.0. DO NOT EDIT.
# This file was automatically generated from an ADL (Agent Definition Language) specification.
# Manual changes to this file may be overwritten during regeneration.

---
name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-24.04
    
    steps:
    - uses: actions/checkout@v7.0.0
    
    - name: Set up Go
      uses: actions/setup-go@v6.5.0
      with:
        go-version-file: go.mod
        cache: true

    - name: Install golangci-lint
      uses: golangci/golangci-lint-action@v9.3.0
      with:
        version: v2.12.2
        args: --help

    - name: Install task
      uses: arduino/setup-task@v3.0.0
      with:
        version: 3.48.0
        repo-token: ${{ secrets.GITHUB_TOKEN }}

    - name: Install ADL CLI
      env:
        VERSION: v0.49.0
      run: |
        curl -fsSL https://raw.githubusercontent.com/inference-gateway/adl-cli/v0.49.0/install.sh | bash
        adl --version

    - name: Validate ADL
      run: task validate

    - name: Download dependencies
      run: go mod download
    
    - name: Format check
      run: task fmt
    
    - name: Lint
      run: task lint
    
    - name: Run tests
      run: task test
    
    - name: Build
      run: task build
